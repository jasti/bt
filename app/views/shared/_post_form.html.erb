<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :question, placeholder: "Ask your question..." %>
  </div>
  <%= f.submit "Ask", class: "btn btn-large btn-primary" %>
<% end %>