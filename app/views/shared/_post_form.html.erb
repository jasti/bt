<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :question, placeholder: "Ask new Question..." %>
  </div>
  <%= f.submit "Ask", class: "btn btn-medium" %>
<% end %>